/** @type {import("eslint").Linter.FlatConfig[]} */
const config = [
    {
        settings: {
            next: {
                rootDir: ['./'],
            },
        },
    },
]

export default config
